# Evolution Log Standard (v0.1)

Without an verifiable log, cognitive growth cannot be audited or rolled back[cite: 1]. Any modification altering memory, skills, or strategies must yield this state capture[cite: 1]:

* It must encapsulate detailed `before` and `after` snapshots[cite: 1].
* It must declare a `rollback_available` state along with an explicit inverse execution recipe to undo changes cleanly[cite: 1].

*(Paste the JSON block from Appendix A.5 of Whitepaper v0.2 here)*[cite: 1]